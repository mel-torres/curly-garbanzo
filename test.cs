using namespace std;

int main () {
   
   int a,b,max;
   
   cout  a; 
   cout  b; 
   
   //remove the terminating character
   std::cin.ignore();
 
   if( ab ) {max=a;}
        else {max=b;}
   
   cout   
   //Extracts characters from the stream
   std::cin.get();
   return 0;
}
